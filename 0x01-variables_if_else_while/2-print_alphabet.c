#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print all alphabet letters
 *
 * Return: 0 (Success)
*/

int main(void)
{
char ch = 'a';
while (ch <= '2')
{
putchar(ch);
ch++;
}
putchar('\n);
return (0);
}
